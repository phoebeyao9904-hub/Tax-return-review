import React from "react";
import ReactDOM from "react-dom/client";
import TaxReturnReview from "../app/page";
import "../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TaxReturnReview />
  </React.StrictMode>,
);
